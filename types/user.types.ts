export type UserInfo = {
      email: string
      password: string,
      question: string,
      answer: string,
}

export type UserAddress = {
      country: string;
      name: string;
      mobile: number;
      zip: string;
      address: string;
      city: string;
      state: string;
}
